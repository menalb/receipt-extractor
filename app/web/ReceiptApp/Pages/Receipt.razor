@page "/receipt/{ReceiptId}"
@using ReceiptApp.Components
@attribute [Microsoft.AspNetCore.Authorization.Authorize]

<EditReceipt ReceiptId="@ReceiptId" />

@code {

    [Parameter]
    public string ReceiptId { get; set; } = "";
}
